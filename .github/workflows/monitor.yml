name: 东横Inn 监控 (v19.0 透明调试版)

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  check_rooms:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v3

      - name: 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 安装依赖
        run: pip install requests beautifulsoup4 resend

      - name: 生成并运行脚本
        run: |
          cat << 'EOF' > monitor.py
          import requests
          from bs4 import BeautifulSoup
          import os
          import resend
          import time
          from urllib.parse import urlparse, parse_qs, unquote
          import json
          import traceback
          from datetime import datetime, timedelta, timezone

          # === 0. 时间控制器 ===
          try:
              tz_beijing = timezone(timedelta(hours=8))
              now = datetime.now(tz_beijing)
              if os.path.exists('run_time.txt'):
                  with open('run_time.txt', 'r') as f:
                      content = f.read().strip()
                      if '-' in content:
                          s, e = map(int, content.split('-'))
                          h = now.hour
                          is_run = (s <= h <= e) if s <= e else (h >= s or h <= e)
                          if not is_run:
                              print(f'💤 休眠时间 ({s}-{e})，当前 {h} 点。')
                              exit(0)
          except: pass

          # === 配置区 ===
          resend.api_key = os.environ.get('RESEND_API_KEY')
          user_email = os.environ.get('USER_EMAIL', '').strip()
          
          headers = {
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
              'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8',
              'Accept-Language': 'zh-CN,zh;q=0.9,en;q=0.8',
              'Referer': 'https://www.toyoko-inn.com/china_cn/',
              'Connection': 'keep-alive',
          }

          urls = []
          try:
              with open('urls.txt', 'r') as f:
                  for line in f:
                      line = line.strip()
                      if line and not line.startswith('#'):
                          urls.append(line)
          except:
              print('❌ 找不到 urls.txt')
              exit(1)

          print(f'\n📋 启动监控: {len(urls)} 个任务 (v19.0 透明调试版)\n')
          
          session = requests.Session()
          session.headers.update(headers)
          
          found_list = []

          for i, url in enumerate(urls):
              try:
                  parsed = urlparse(url)
                  params = parse_qs(parsed.query)
                  hotel_id = params.get('hotel', ['未知'])[0]
                  checkin_date = params.get('start', ['未知'])[0]
                  
                  max_price = 999999
                  if 'max' in params:
                      try: max_price = int(params['max'][0])
                      except: pass
                  
                  target_room = None
                  if 'roomtype' in params: target_room = unquote(params['roomtype'][0])
                  
                  target_smoke = None 
                  if 'smoke' in params:
                      val = params['smoke'][0]
                      if val == '1': target_smoke = True
                      elif val == '0': target_smoke = False
              except:
                  hotel_id = '未知'
                  checkin_date = '未知'
                  max_price = 999999
                  target_room = None
                  target_smoke = None

              info = []
              if max_price < 999999: info.append(f'<{max_price}')
              if target_room: info.append(f'[{target_room}]')
              if target_smoke is True: info.append('[吸烟]')
              elif target_smoke is False: info.append('[禁烟]')
              
              print(f'[{i+1}/{len(urls)}] 🏨 {hotel_id} | 📅 {checkin_date} | {", ".join(info) or "无筛选"}')

              for attempt in range(2):
                  try:
                      resp = session.get(url, timeout=30)
                      
                      # 维护/JSON检测
                      if '__NEXT_DATA__' not in resp.text:
                          print(f'      ⚠️ 无 JSON 数据 (可能被拦截)')
                          continue

                      soup = BeautifulSoup(resp.text, 'html.parser')
                      script = soup.find('script', id='__NEXT_DATA__')
                      data = json.loads(script.string)
                      page_props = data.get('props', {}).get('pageProps', {})
                      room_plan_list = page_props.get('roomPlanList', [])
                      if room_plan_list is None: room_plan_list = []

                      # === 🔥 DEBUG 核心：打印我看到了什么 ===
                      print(f'      🔎 JSON 解析成功，共找到 {len(room_plan_list)} 个套餐')
                      
                      valid_rooms = []
                      
                      for plan in room_plan_list:
                          room_types = plan.get('roomTypes', [])
                          for room in room_types:
                              r_name = room.get('roomTypeName', '')
                              specs = room.get('specs', {})
                              is_smoking = specs.get('isSmoking', False)
                              
                              vacant = room.get('vacant', {})
                              stock = vacant.get('generalVacantRoom', 0)
                              if stock is None: stock = 0
                              
                              price_obj = room.get('price', {})
                              price = price_obj.get('generalPrice', 999999)
                              
                              # 构建调试信息
                              smoke_str = '吸烟' if is_smoking else '禁烟'
                              room_desc = f"【{r_name}】{smoke_str} 剩{stock}间 ¥{price}"
                              
                              # --- 逐步检查筛选条件，并打印被谁过滤了 ---
                              reject_reason = None
                              
                              if stock <= 0: 
                                  reject_reason = "❌ 无库存"
                              elif price > max_price: 
                                  reject_reason = f"❌ 太贵 (>{max_price})"
                              elif target_room and target_room not in r_name: 
                                  reject_reason = f"❌ 房型不符 (搜[{target_room}])"
                              elif target_smoke is not None and target_smoke != is_smoking: 
                                  target_str = '吸烟' if target_smoke else '禁烟'
                                  reject_reason = f"❌ 烟味不符 (搜[{target_str}])"
                              
                              if reject_reason:
                                  # 只有当这是目标房型但因其他原因失败时，才打印详细日志，避免刷屏
                                  if target_room and target_room in r_name:
                                       print(f'      {room_desc} -> {reject_reason}')
                                  # 如果完全没指定房型，那就打印所有有库存的
                                  elif stock > 0 and not target_room:
                                       print(f'      {room_desc} -> {reject_reason}')
                              else:
                                  print(f'      ✅ 匹配成功! {room_desc}')
                                  valid_rooms.append({
                                      'name': r_name,
                                      'smoke': smoke_str,
                                      'price': price,
                                      'stock': stock,
                                      'plan': plan.get('planName', '标准')
                                  })

                      if valid_rooms:
                          best = min(valid_rooms, key=lambda x: x['price'])
                          print(f"      🎉 最终锁定: {best['name']}({best['smoke']}) ¥{best['price']}")
                          
                          hotel_name = f'酒店{hotel_id}'
                          try: hotel_name = page_props['hotel']['hotelName']
                          except: pass

                          found_list.append({
                              'name': hotel_name,
                              'date': checkin_date,
                              'url': url,
                              'room': best['name'],
                              'smoke': best['smoke'],
                              'price': best['price'],
                              'details': valid_rooms
                          })
                          break 
                      else:
                          print('      😴 扫描完毕，无有效目标')
                          break

                  except Exception as e:
                      print(f'      ❌ 错误: {e}')
                      time.sleep(2)
              
              print('-'*40)
              time.sleep(2)

          if found_list:
              print(f'\n📧 发送邮件 ({len(found_list)}个目标)...')
              if user_email and resend.api_key:
                  try:
                      first = found_list[0]
                      subj = f"【有房】{first['name']} {first['date']} {first['room']}"
                      body = "<h3>🎉 发现目标：</h3>"
                      for item in found_list:
                          body += f"<hr><h4>{item['name']} ({item['date']})</h4>"
                          body += f"<p>最低: <span style='color:red'>¥{item['price']}</span></p>"
                          body += f"<p><a href='{item['url']}'>👉 立即预订</a></p><ul>"
                          dedup = set()
                          for d in item['details']:
                              key = f"{d['name']}-{d['smoke']}-{d['price']}"
                              if key not in dedup:
                                  dedup.add(key)
                                  body += f"<li>{d['name']} ({d['smoke']}) ¥{d['price']} 剩{d['stock']}</li>"
                          body += "</ul>"
                      
                      resend.Emails.send({'from': 'onboarding@resend.dev', 'to': user_email, 'subject': subj, 'html': body})
                      print('✅ 发送成功')
                  except Exception as e:
                      print(f'❌ 发送失败: {e}')
              else:
                  print('❌ 邮箱未配置')
          else:
              print('\n🏁 监控结束')
          EOF
          
          python -u monitor.py
