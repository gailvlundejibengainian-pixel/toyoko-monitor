name: 东横Inn 监控 (价格筛选最终版)

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  check_rooms:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v3

      - name: 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 安装依赖
        run: pip install requests beautifulsoup4 resend

      - name: 批量运行监控
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          USER_EMAIL: ${{ secrets.USER_EMAIL }}
        run: |
          python -c "
          import requests
          from bs4 import BeautifulSoup
          import os
          import resend
          import time
          from urllib.parse import urlparse, parse_qs
          import re

          # 1. 基础配置
          resend.api_key = os.environ.get('RESEND_API_KEY')
          raw_email = os.environ.get('USER_EMAIL', '')
          user_email = raw_email.strip()

          headers = {
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
              'Accept-Language': 'zh-CN,zh;q=0.9', 
          }

          try:
              with open('urls.txt', 'r') as f:
                  urls = [line.strip() for line in f if line.strip()]
          except FileNotFoundError:
              print('❌ 错误：找不到 urls.txt')
              exit(1)

          print(f'📋 启动监控: {len(urls)} 个任务 (含比价功能)\n')
          
          found_list = []

          for i, url in enumerate(urls):
              # 解析 URL 参数
              try:
                  parsed = urlparse(url)
                  params = parse_qs(parsed.query)
                  hotel_id = params.get('hotel', ['未知'])[0]
                  checkin_date = params.get('start', ['未知'])[0]
                  
                  # === 获取用户设定的价格上限 ===
                  max_price_limit = None
                  if 'max' in params:
                      try:
                          max_price_limit = int(params['max'][0])
                      except: pass
              except:
                  hotel_id, checkin_date = '未知', '未知'
                  max_price_limit = None

              limit_info = f' (限价: ¥{max_price_limit})' if max_price_limit else ''
              print(f'[{i+1}/{len(urls)}] 🏨 {hotel_id} | 📅 {checkin_date}{limit_info}')
              
              try:
                  resp = requests.get(url, headers=headers, timeout=30)
                  text_content = resp.text
                  
                  # === 1. 提取页面所有有效价格 (自动去逗号) ===
                  found_prices = []
                  raw_prices = re.findall(r'¥\s*([0-9,]+)', text_content)
                  for p in raw_prices:
                      try:
                          val = int(p.replace(',', ''))
                          if val != 10000: # 排除干扰项
                              found_prices.append(val)
                      except: pass
                  
                  # 找出最低价
                  min_price = min(found_prices) if found_prices else 999999
                  
                  # === 2. 判定是否有房 ===
                  # 只要有库存、或者有预订按钮、或者有真实价格，就算有房
                  has_stock = re.search(r'剩\s*\d+\s*间', text_content)
                  has_btn = 'reserve/input' in text_content or 'value=\"预订\"' in text_content
                  
                  is_available = False
                  status_msg = '😴 暂无空房'

                  # 只有当确实检测到有房迹象时，才进入价格判断
                  if has_stock or has_btn or (found_prices and '没有空房' not in text_content):
                      # 价格过滤逻辑
                      if max_price_limit:
                          if min_price <= max_price_limit:
                              is_available = True
                              status_msg = f'🎉 发现空房且价格达标! (最低 ¥{min_price})'
                          else:
                              status_msg = f'💸 有房但太贵 (最低 ¥{min_price} > 限价 ¥{max_price_limit})'
                      else:
                          # 没设限价，直接报喜
                          is_available = True
                          price_show = f'¥{min_price}' if min_price < 999999 else '未知'
                          status_msg = f'🎉 发现空房! (最低 {price_show})'
                  
                  print(f'      {status_msg}')

                  if is_available:
                      found_list.append({
                          'id': hotel_id,
                          'date': checkin_date,
                          'url': url,
                          'price': min_price if min_price < 999999 else 0
                      })
                  
                  time.sleep(2)

              except Exception as e:
                  print(f'      ❌ 出错: {e}')
              print('-'*40)

          # 发送邮件
          if found_list:
              print(f'\n📧 发现 {len(found_list)} 个符合条件的目标...')
              
              if not user_email or '@' not in user_email:
                  print('❌ USER_EMAIL 未配置')
              elif not resend.api_key:
                  print('❌ RESEND_API_KEY 未配置')
              else:
                  first = found_list[0]
                  p_title = f'¥{first["price"]}' if first["price"] > 0 else '有房'
                  subject_txt = f'【有房】{first["date"]} {p_title}'
                  
                  email_body = '<h3>🎉 发现符合预期的空房：</h3><ul>'
                  for item in found_list:
                      p_str = f'¥{item["price"]}' if item["price"] > 0 else '有空房'
                      email_body += f'<li><strong>{item["date"]} (店号{item["id"]})</strong> | 价格: <span style="color:red">{p_str}</span><br><a href=\"{item["url"]}\">👉 点击立即预订</a></li>'
                  email_body += '</ul>'
                  
                  try:
                      resend.Emails.send({
                        'from': 'onboarding@resend.dev',
                        'to': user_email,
                        'subject': subject_txt,
                        'html': email_body
                      })
                      print('✅ 邮件发送成功！')
                  except Exception as e:
                      print(f'❌ 邮件发送失败: {e}')
          else:
              print('\n🏁 监控结束，无符合条件房源。')
          "
