name: 东横Inn 监控 (严格逻辑版)

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  check_rooms:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v3

      - name: 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 安装依赖
        run: pip install requests beautifulsoup4 resend

      - name: 批量运行监控
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          USER_EMAIL: ${{ secrets.USER_EMAIL }}
        run: |
          python -c "
          import requests
          from bs4 import BeautifulSoup
          import os
          import resend
          import time
          from urllib.parse import urlparse, parse_qs
          import re
          import traceback

          # 1. 基础配置
          resend.api_key = os.environ.get('RESEND_API_KEY')
          raw_email = os.environ.get('USER_EMAIL', '')
          user_email = raw_email.strip()

          headers = {
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
              'Accept-Language': 'zh-CN,zh;q=0.9', 
          }

          try:
              with open('urls.txt', 'r') as f:
                  urls = [line.strip() for line in f if line.strip()]
          except FileNotFoundError:
              print('❌ 错误：找不到 urls.txt')
              exit(1)

          print(f'📋 启动监控: {len(urls)} 个任务 (逻辑: 仅当抓到价格或库存时才算有房)\n')
          
          found_list = []

          for i, url in enumerate(urls):
              try:
                  parsed = urlparse(url)
                  params = parse_qs(parsed.query)
                  hotel_id = params.get('hotel', ['未知'])[0]
                  checkin_date = params.get('start', ['未知'])[0]
                  
                  max_limit = 999999
                  if 'max' in params:
                      try: max_limit = int(params['max'][0])
                      except: pass
                  
                  min_limit = 5000 
                  if 'min' in params:
                      try: min_limit = int(params['min'][0])
                      except: pass
              except:
                  hotel_id, checkin_date = '未知', '未知'
                  max_limit = 999999
                  min_limit = 5000

              limit_info = f' (预算: {min_limit}~{max_limit})'
              print(f'[{i+1}/{len(urls)}] 🏨 {hotel_id} | 📅 {checkin_date}{limit_info}')
              
              try:
                  resp = requests.get(url, headers=headers, timeout=30)
                  
                  # === 1. 清洗网页 ===
                  soup = BeautifulSoup(resp.text, 'html.parser')
                  for trash in soup(['script', 'style', 'noscript', 'iframe', 'header', 'footer', 'nav']):
                      trash.decompose()
                  for div in soup.find_all('div', class_=re.compile(r'footer|header|banner|advert')):
                      div.decompose()

                  clean_text = soup.get_text()
                  
                  # === 2. 提取价格 ===
                  valid_prices = []
                  raw_matches = re.findall(r'¥\s*([0-9,]+)', clean_text)
                  
                  for p in raw_matches:
                      try:
                          val = int(p.replace(',', ''))
                          if val != 10000 and val >= min_limit: 
                              valid_prices.append(val)
                      except: pass
                  
                  real_min_price = min(valid_prices) if valid_prices else 999999
                  
                  # === 3. 判定是否有房 (逻辑修正) ===
                  has_stock = re.search(r'剩\s*\d+\s*间', clean_text)
                  
                  is_available = False
                  status_msg = '😴 暂无空房'

                  # 🔥 核心修正 🔥
                  # 以前是: 只要看见“预订”二字就算有房 -> 导致误判
                  # 现在是: 必须【抓到了有效价格】或者【看见了“剩X间”】才算有房
                  
                  if valid_prices:
                      # 抓到了真实价格，说明真的有房
                      if real_min_price <= max_limit:
                          is_available = True
                          status_msg = f'🎉 发现空房且价格合适! (最低 ¥{real_min_price})'
                      else:
                          status_msg = f'💸 有房但太贵 (最低 ¥{real_min_price} > 预算 {max_limit})'
                  
                  elif has_stock:
                      # 没抓到价格，但是看到了“剩 1 间”，可能是价格格式变了，先报警
                      is_available = True
                      status_msg = f'🎉 发现空房! (库存提示: {has_stock.group()}, 价格未识别)'
                  
                  else:
                      # 既没价格，也没库存 -> 那就是真的没房
                      status_msg = '😴 暂无空房'
                  
                  print(f'      {status_msg}')

                  if is_available:
                      found_list.append({
                          'id': hotel_id,
                          'date': checkin_date,
                          'url': url,
                          'price': real_min_price if real_min_price < 999999 else 0
                      })
                  
                  time.sleep(2)

              except Exception as e:
                  print(f'      ❌ 出错: {e}')
              print('-'*40)

          # 发送邮件
          if found_list:
              print(f'\n📧 发现 {len(found_list)} 个符合条件的目标...')
              
              if not user_email or '@' not in user_email:
                  print('❌ USER_EMAIL 未配置')
              elif not resend.api_key:
                  print('❌ RESEND_API_KEY 未配置')
              else:
                  try:
                      first = found_list[0]
                      p_val = first['price']
                      checkin_str = first['date'] 
                      
                      p_title = f'¥{p_val}' if p_val > 0 else '有房'
                      subject_txt = f'【有房】{checkin_str} {p_title} (符合预算)'
                      
                      email_body = '<h3>🎉 发现符合预算的空房：</h3><ul>'
                      for item in found_list:
                          i_price = item["price"]
                          p_str = f'¥{i_price}' if i_price > 0 else '有空房'
                          email_body += f'<li><strong>{item["date"]} (店号{item["id"]})</strong><br>价格: <span style="color:red">{p_str}</span><br><a href=\"{item["url"]}\">👉 点击立即预订</a></li>'
                      email_body += '</ul>'
                  
                      resend.Emails.send({
                        'from': 'onboarding@resend.dev',
                        'to': user_email,
                        'subject': subject_txt,
                        'html': email_body
                      })
                      print('✅ 邮件发送成功！')
                  except Exception as e:
                      print(f'❌ 邮件发送失败: {e}')
                      traceback.print_exc()
          else:
              print('\n🏁 监控结束，无符合条件房源。')
          "
