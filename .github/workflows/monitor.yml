name: 东横Inn 监控 (v23.0 双引擎列表版)

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  check_rooms:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v3

      - name: 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 安装依赖
        run: pip install requests beautifulsoup4 resend

      - name: 生成并运行脚本
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          USER_EMAIL: ${{ secrets.USER_EMAIL }}
        run: |
          cat << 'EOF' > monitor.py
          import requests
          from bs4 import BeautifulSoup
          import os
          import resend
          import time
          from urllib.parse import urlparse, parse_qs, unquote
          import json
          import re
          from datetime import datetime, timedelta, timezone

          # === 1. 时间控制器 ===
          try:
              tz_beijing = timezone(timedelta(hours=8))
              now = datetime.now(tz_beijing)
              if os.path.exists('run_time.txt'):
                  with open('run_time.txt', 'r') as f:
                      content = f.read().strip()
                      if '-' in content:
                          s, e = map(int, content.split('-'))
                          h = now.hour
                          is_run = (s <= h <= e) if s <= e else (h >= s or h <= e)
                          if not is_run:
                              print(f'💤 休眠时间 ({s}-{e})，当前 {h} 点。')
                              exit(0)
          except: pass

          # === 2. 基础配置 ===
          resend.api_key = os.environ.get('RESEND_API_KEY')
          user_email = os.environ.get('USER_EMAIL', '').strip()
          
          headers = {
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36',
              'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8',
              'Referer': 'https://www.toyoko-inn.com/china_cn/',
          }

          urls = []
          try:
              with open('urls.txt', 'r') as f:
                  for line in f:
                      line = line.strip()
                      if line and not line.startswith('#'):
                          urls.append(line)
          except:
              print('❌ 找不到 urls.txt')
              exit(1)

          print(f'\n📋 启动监控: {len(urls)} 个任务 (双引擎列表版)\n')
          
          session = requests.Session()
          session.headers.update(headers)
          
          # 尝试预热 Session
          try: session.get('https://www.toyoko-inn.com/china_cn/', timeout=5)
          except: pass

          found_list = []

          for i, url in enumerate(urls):
              # 参数解析
              try:
                  parsed = urlparse(url)
                  params = parse_qs(parsed.query)
                  hotel_id = params.get('hotel', ['未知'])[0]
                  checkin_date = params.get('start', ['未知'])[0]
                  
                  max_price = 999999
                  if 'max' in params:
                      try: max_price = int(params['max'][0])
                      except: pass
                  
                  target_room = None
                  if 'roomtype' in params: target_room = unquote(params['roomtype'][0])
                  
                  target_smoke = None 
                  if 'smoke' in params:
                      val = params['smoke'][0]
                      if val == '1': target_smoke = True
                      elif val == '0': target_smoke = False
              except:
                  hotel_id = '未知'
                  checkin_date = '未知'
                  max_price = 999999
                  target_room = None
                  target_smoke = None

              print(f'[{i+1}/{len(urls)}] 🏨 {hotel_id} | 📅 {checkin_date}')

              # === 双引擎启动 ===
              detailed_rooms = [] # 存放详细房型列表
              text_min_price = None # 存放文本扫描到的最低价
              
              try:
                  resp = session.get(url, timeout=30)
                  
                  # 引擎 A: JSON 解析 (获取详细清单)
                  if '__NEXT_DATA__' in resp.text:
                      try:
                          soup = BeautifulSoup(resp.text, 'html.parser')
                          script = soup.find('script', id='__NEXT_DATA__')
                          data = json.loads(script.string)
                          room_plan_list = data.get('props', {}).get('pageProps', {}).get('roomPlanList', [])
                          if room_plan_list is None: room_plan_list = []
                          
                          hotel_name_json = data.get('props', {}).get('pageProps', {}).get('hotel', {}).get('hotelName', '')
                          
                          for plan in room_plan_list:
                              room_types = plan.get('roomTypes', [])
                              for room in room_types:
                                  r_name = room.get('roomTypeName', '')
                                  specs = room.get('specs', {})
                                  is_smoking = specs.get('isSmoking', False)
                                  
                                  vacant = room.get('vacant', {})
                                  stock = vacant.get('generalVacantRoom', 0)
                                  if stock is None: stock = 0
                                  
                                  price_obj = room.get('price', {})
                                  price = price_obj.get('generalPrice', 999999)
                                  
                                  # 筛选逻辑
                                  if stock <= 0: continue
                                  if price > max_price: continue
                                  if target_room and target_room not in r_name: continue
                                  if target_smoke is not None and target_smoke != is_smoking: continue
                                  
                                  # 如果通过筛选，加入列表
                                  smoke_str = '吸烟' if is_smoking else '禁烟'
                                  detailed_rooms.append({
                                      'name': r_name,
                                      'smoke': smoke_str,
                                      'price': price,
                                      'stock': stock
                                  })
                      except: pass

                  # 引擎 B: 文本扫描 (兜底)
                  # 如果 JSON 引擎没找到任何东西(可能是参数不匹配，也可能是被拦截)，我们用文本再扫一次
                  if not detailed_rooms:
                      soup = BeautifulSoup(resp.text, 'html.parser')
                      # 粗暴去噪
                      for t in soup(['script', 'style', 'header', 'footer']): t.decompose()
                      clean_text = soup.get_text()
                      
                      # 找价格
                      raw_prices = re.findall(r'¥\s*([0-9,]+)', clean_text)
                      valid_prices = []
                      for p in raw_prices:
                          try:
                              v = int(p.replace(',', ''))
                              if v > 4000 and v != 10000: valid_prices.append(v)
                          except: pass
                      
                      if valid_prices:
                          min_p = min(valid_prices)
                          if min_p <= max_price:
                              text_min_price = min_p

                  # === 决策时刻 ===
                  final_item = None
                  
                  if detailed_rooms:
                      # 场景 1: JSON 立功了，有详细列表
                      best = min(detailed_rooms, key=lambda x: x['price'])
                      print(f'      🎉 发现 {len(detailed_rooms)} 个房型! 最低: {best["name"]} ¥{best["price"]}')
                      final_item = {
                          'id': hotel_id,
                          'name': hotel_name_json if 'hotel_name_json' in locals() else f'酒店{hotel_id}',
                          'date': checkin_date,
                          'url': url,
                          'price': best['price'],
                          'details': detailed_rooms, # 关键：把列表传给邮件
                          'type': 'json'
                      }
                      
                  elif text_min_price:
                      # 场景 2: JSON 没戏，但文本扫到了价格
                      print(f'      🎉 文本扫描发现价格! 最低 ¥{text_min_price}')
                      final_item = {
                          'id': hotel_id,
                          'name': f'酒店{hotel_id}',
                          'date': checkin_date,
                          'url': url,
                          'price': text_min_price,
                          'details': [], # 空列表
                          'type': 'text'
                      }
                  else:
                      print('      😴 暂无符合条件的空房')

                  if final_item:
                      found_list.append(final_item)
                  
                  time.sleep(2)

              except Exception as e:
                  print(f'      ❌ 出错: {e}')

              print('-'*40)

          # === 发送邮件 (增强版) ===
          if found_list:
              print(f'\n📧 发送邮件 ({len(found_list)}个目标)...')
              if user_email and resend.api_key:
                  try:
                      first = found_list[0]
                      subj = f"【有房】{first['name']} {first['date']} ¥{first['price']}"
                      
                      body = "<h3>🎉 发现空房清单：</h3>"
                      
                      for item in found_list:
                          body += f"<hr>"
                          body += f"<h4>🏨 {item['name']} ({item['date']})</h4>"
                          body += f"<p><a href='{item['url']}'>👉 点击立即预订</a></p>"
                          
                          if item['type'] == 'json' and item['details']:
                              # 如果有详细列表，做成表格
                              body += "<table border='1' cellpadding='5' style='border-collapse:collapse;'>"
                              body += "<tr style='background:#f0f0f0'><th>房型</th><th>吸烟</th><th>价格</th><th>库存</th></tr>"
                              
                              # 简单去重 (防止同一个房型出现多次)
                              seen = set()
                              for d in item['details']:
                                  key = f"{d['name']}-{d['smoke']}-{d['price']}"
                                  if key not in seen:
                                      seen.add(key)
                                      body += f"<tr><td>{d['name']}</td><td>{d['smoke']}</td><td style='color:red'>¥{d['price']}</td><td>剩{d['stock']}间</td></tr>"
                              body += "</table>"
                          else:
                              # 如果只有文本兜底
                              body += f"<p><strong>最低检测价格:</strong> <span style='color:red'>¥{item['price']}</span></p>"
                              body += "<p style='color:gray;font-size:12px'>(仅通过文本扫描发现，具体房型请点击链接确认)</p>"
                      
                      resend.Emails.send({'from': 'onboarding@resend.dev', 'to': user_email, 'subject': subj, 'html': body})
                      print('✅ 邮件发送成功！')
                  except Exception as e:
                      print(f'❌ 邮件发送失败: {e}')
              else:
                  print('❌ 邮箱未配置')
          else:
              print('\n🏁 监控结束，无符合条件房源。')
          EOF
          
          python -u monitor.py
