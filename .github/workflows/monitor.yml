name: 东横Inn 监控 (修复版+价格侦探)

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  check_rooms:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v3

      - name: 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 安装依赖
        run: pip install requests beautifulsoup4 resend

      - name: 批量运行监控
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          USER_EMAIL: ${{ secrets.USER_EMAIL }}
        run: |
          python -c "
          import requests
          from bs4 import BeautifulSoup
          import os
          import resend
          import time
          from urllib.parse import urlparse, parse_qs
          import re
          import traceback

          # 1. 基础配置
          resend.api_key = os.environ.get('RESEND_API_KEY')
          raw_email = os.environ.get('USER_EMAIL', '')
          user_email = raw_email.strip()

          headers = {
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
              'Accept-Language': 'zh-CN,zh;q=0.9', 
          }

          try:
              with open('urls.txt', 'r') as f:
                  urls = [line.strip() for line in f if line.strip()]
          except FileNotFoundError:
              print('❌ 错误：找不到 urls.txt')
              exit(1)

          print(f'📋 启动监控: {len(urls)} 个任务 (逻辑: 5000~Max, 修复报错)\n')
          
          found_list = []

          for i, url in enumerate(urls):
              # 解析 URL 参数
              try:
                  parsed = urlparse(url)
                  params = parse_qs(parsed.query)
                  hotel_id = params.get('hotel', ['未知'])[0]
                  checkin_date = params.get('start', ['未知'])[0]
                  
                  # === 获取 Max 价格 ===
                  max_limit = 999999
                  if 'max' in params:
                      try:
                          max_limit = int(params['max'][0])
                      except: pass
                  
                  # === 获取 Min 价格 (默认5000) ===
                  min_limit = 5000 
                  if 'min' in params:
                      try:
                          min_limit = int(params['min'][0])
                      except: pass
                      
              except:
                  hotel_id, checkin_date = '未知', '未知'
                  max_limit = 999999
                  min_limit = 5000

              limit_info = f' (预算: {min_limit}~{max_limit})'
              print(f'[{i+1}/{len(urls)}] 🏨 {hotel_id} | 📅 {checkin_date}{limit_info}')
              
              try:
                  resp = requests.get(url, headers=headers, timeout=30)
                  text_content = resp.text
                  
                  # === 1. 提取并清洗价格 ===
                  valid_prices = []
                  raw_matches = re.findall(r'¥\s*([0-9,]+)', text_content)
                  
                  # DEBUG: 打印它到底抓到了什么
                  debug_prices = [] 

                  for p in raw_matches:
                      try:
                          val = int(p.replace(',', ''))
                          debug_prices.append(val)
                          
                          # 过滤逻辑: 排除 10000 (干扰项) 和 低于 5000 的杂费
                          if val != 10000 and val >= min_limit: 
                              valid_prices.append(val)
                      except: pass
                  
                  # 打印价格侦探日志 (让你看清 7600 是哪来的)
                  print(f'      🔎 原始抓取: {debug_prices}')
                  print(f'      🧹 清洗之后: {valid_prices}')
                  
                  # 找出清洗后的最低价
                  real_min_price = min(valid_prices) if valid_prices else 999999
                  
                  # === 2. 判定是否有房 ===
                  has_stock = re.search(r'剩\s*\d+\s*间', text_content)
                  has_btn = 'reserve/input' in text_content or 'value=\"预订\"' in text_content
                  
                  is_available = False
                  status_msg = '😴 暂无空房'

                  if has_stock or has_btn or (valid_prices and '没有空房' not in text_content):
                      if real_min_price <= max_limit:
                          is_available = True
                          price_show = f'¥{real_min_price}' if real_min_price < 999999 else '价格未知'
                          status_msg = f'🎉 发现空房且价格合适! (最低 {price_show})'
                      else:
                          status_msg = f'💸 有房但太贵 (最低 ¥{real_min_price} > 预算 {max_limit})'
                  
                  print(f'      {status_msg}')

                  if is_available:
                      found_list.append({
                          'id': hotel_id,
                          'date': checkin_date,
                          'url': url,
                          'price': real_min_price if real_min_price < 999999 else 0
                      })
                  
                  time.sleep(2)

              except Exception as e:
                  print(f'      ❌ 出错: {e}')
              print('-'*40)

          # 发送邮件 (修复 price 变量未定义问题)
          if found_list:
              print(f'\n📧 发现 {len(found_list)} 个符合条件的目标...')
              
              if not user_email or '@' not in user_email:
                  print('❌ USER_EMAIL 未配置')
              elif not resend.api_key:
                  print('❌ RESEND_API_KEY 未配置')
              else:
                  try:
                      first = found_list[0]
                      # 修复点：这里原来可能写错了，现在统一用 first['price']
                      p_val = first['price']
                      p_title = f'¥{p_val}' if p_val > 0 else '有房'
                      subject_txt = f'【有房】{first["date"]} {p_title} (符合预算)'
                      
                      email_body = '<h3>🎉 发现符合预算的空房：</h3><ul>'
                      for item in found_list:
                          # 修复点：确保这里用 item['price']
                          i_price = item['price']
                          p_str = f'¥{i_price}' if i_price > 0 else '有空房'
                          email_body += f'<li><strong>{item["date"]} (店号{item["id"]})</strong><br>价格: <span style="color:red">{p_str}</span><br><a href=\"{item["url"]}\">👉 点击立即预订</a></li>'
                      email_body += '</ul>'
                  
                      resend.Emails.send({
                        'from': 'onboarding@resend.dev',
                        'to': user_email,
                        'subject': subject_txt,
                        'html': email_body
                      })
                      print('✅ 邮件发送成功！')
                  except Exception as e:
                      print(f'❌ 邮件发送失败: {e}')
                      traceback.print_exc()
          else:
              print('\n🏁 监控结束，无符合条件房源。')
          "
